#ifndef SORT_CONTAINER_H
#define SORT_CONTAINER_H

typedef struct sort_container{
  int value, /**< Actual value to be sorted by. */
      index; /**< Value which corresponds to the index value before sorting. */
} sort_container;


#endif /* #ifndef SORT_CONTAINER_H */
